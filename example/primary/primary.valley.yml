types:
  Example:
    constraints:
    - name: MutuallyExclusive
      opts: [Text, Texts, TextMap]

    fields:
      Text:
        constraints:
        - name: Required
      Int:
        constraints:
        - name: Required
      Ints:
        constraints:
        - name: Required
        elements:
          constraints:
          - name: Required

  NestedExample:
    fields:
      Text:
        constraints:
        - name: Required
